//! Placeholder de extracción de metadata en PDFs.

use std::path::Path;

/// Implementación simplificada hasta contar con soporte completo.
pub fn extract_pdf_metadata(_path: &Path) -> bool {
    false
}
